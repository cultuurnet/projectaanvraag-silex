<div class="cnw_w" data-widget-id="{{ id }}">
    <div class="cnw_container">
        <div id="loadcontent">
            {% for facet in facets %}
                {% if facet.count > 0 %}
                    {% include 'widgets/facet.html.twig' with { 'facet': facet } %}
                {% endif %}
            {% endfor %}
        </div>
    </div>
</div>